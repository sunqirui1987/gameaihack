# AGENTS.md

这是 `{{ package_name }}` 的 **TapTap Maker** 工程（分析产出的 `output/`）。

用 Cindy 打开本目录。走 Maker 插件做游戏，不要重打包原 APK，不要当普通 Git 仓库提交。

## 目录

```
./
  AGENTS.md
  scripts/main.lua     第一局
  assets/image/        上架图
  策划/                说明书
  美术/                对照图
  engine-docs/         Maker 引擎知识（分析用户代码时忽略）
```

`../raw/` 是解包，**不要发给别人**。

## 规则

1. 先读 `策划/00-封面.md`、`策划/02-核心玩法.md`、`策划/图鉴/`。
2. 在 `scripts/` 实现；预览用 `maker_build`。
3. `美术/` 只对照。上架图生成到 `assets/image/`。
4. 禁止盗版客户端、外挂、重打包 APK。
5. Lua 不要用 `os.clock()`、`io.*`。

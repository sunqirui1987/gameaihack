# {{ package_name }} — 游戏解剖包

> 这是分析产物，**不是**游戏安装包，也**不是**可商用素材。  
> 工具：gameaihack {{ tool_version }}　job：`{{ job_id }}`　模式：{{ mode }}

## 30 秒结论

{{ one_liner }}

- 包名 `{{ package_name }}`　版本 {{ version_name }}（{{ version_code }}）
- 引擎 {{ engine }} / {{ script_backend }}
- 品类 {{ genre }} · 资源 {{ n_res }} · 表 {{ n_tb }} · 关卡 {{ n_lv }}
- 输入完整度 {{ input_score }}/100
{% if incomplete %}
- **警告：残缺输入，资源与关卡尚未抽取。**
{% endif %}

## 完整度（0–5）

| 维 | 分 |
|---|---|
{% for r in radar -%}
| {{ r.label }} | {{ r.score }}/{{ r.max }} |
{% endfor %}

## 按角色打开

| 你是 | 先看 |
|---|---|
| 制作人 | [COVER.md](COVER.md) |
| 策划 | [design/](design/) |
| 程序 | [machine/](machine/) · 本页的引擎/保护信息 |
| 用脚本/AI 接 | [machine/game.ir.json](machine/game.ir.json) |

总览：[index.html](index.html)（若空白见 `OPEN.txt`）

## 不要做

- 不要二次分发原画或本包当素材上架
- 不要把 `jobs/` 工作区（可能含原包）发到网盘
- Phase 0 不要假设「没有关卡就是游戏没有关卡」

权利说明：[LEGAL.txt](LEGAL.txt)

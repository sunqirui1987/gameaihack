# 开发

```
cli.py                 analyze / build / doctor
pipeline/run.py        解包 → 美术 → agent → 收口
ingest/ fingerprint/ extract/ content/   机器解包与索引
art/                   提取美术到 output/assets/
agent/prompts/         唯一任务文案（mission.py）
agent/drivers/         sdk / grok / codex / dsh
agent/run.py           写 PRD + 做成游戏
publish/maker.py       init Maker、打包 zip
publish/kit.py         交差封口
```

换抽图：实现 `ArtRipper.rip`。换通道：登记 `agent/drivers/registry.py`。

# 和 llm_bench / Codex 兼容的 OpenAI 接口。
# 已 export 的环境变量优先，这里只补空值。

LLM_API_KEY=sk-xxxx
LLM_BASE_URL=http://127.0.0.1:8080
LLM_MODELS=grok-4.6

# 也认 OPENAI_API_KEY / OPENAI_BASE_URL / DEEPSEEK_API_KEY / DEEPSEEK_BASE_URL
# DSH_MODEL=grok-4.6
